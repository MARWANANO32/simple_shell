#include "shell.h"

/**
 * hs - the main of shell
 * @sk: the parameter , the sk struct
 * @av: the arguments vector
 * Return: if true 0, if error 1
 */
int hs(AWS *sk, char **av)
{
	ssize_t s = 0;
	int builtin_s = 0;

	while (s != -1 && builtin_s != -2)
	{

	}
}
